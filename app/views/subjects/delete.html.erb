<% @page_title = "Delete Subject" %>

<%= link_to('<< Back to List',{action:'index'},class:'btn btn-sm btn-dark') %>

<div class="subject delete">
	<h2>Delete Subject</h2>

	<%= form_for(:subject,url:{action:'destroy',id:@subject.id}) do |f| %>

		<p>Are you sure delete this Subject?</p>
		<p class="reference-name"><%= @subject.name %></p>
		<div class="form-buttons">
			<%= submit_tag('Delete Subject', class:'btn btn-danger') %>
		</div>

	<% end %>
</div>